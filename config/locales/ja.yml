ja:
  date:
    formats:
      default: "%Y/%m/%d"

    order:
    - :year
    - :month
    - :day

  errors:
    format: "%{attribute} %{message}"
    messages:
      blank: を入力してください
      invalid: は不正な値です
      taken: はすでに存在します

  activerecord:
    errors:
      template:
        header: "%{count}件のエラーがあります"

  placeholder:
    partial_match: 部分一致

  template:
    edit: 編集
    destroy: 削除
    save: 保存

  controller:
    errors:
      template:
        csv_export:
          not_selected: CSV出力する要求元整理番号を選択して下さい
